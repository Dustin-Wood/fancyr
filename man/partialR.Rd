% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partialR.R
\name{partialR}
\alias{partialR}
\title{Partial Correlations of Predictors with One or More Outcomes}
\usage{
partialR(
  data,
  predictors,
  dv,
  controls = NULL,
  adjust = "none",
  include_controls = FALSE
)
}
\arguments{
\item{data}{A data frame containing all variables.}

\item{predictors}{Character vector of predictor variable names.}

\item{dv}{Character vector of one or more outcome (dependent) variable names.}

\item{controls}{Character vector of control variable names whose variance is removed
from all other variables before correlating, or \code{NULL} (default) for zero-order
correlations.}

\item{adjust}{Method for p-value correction, passed to \code{\link[psych]{corr.test}}
(zero-order case) or \code{\link[psych]{corr.p}} (partial case). Default is
\code{"none"}. Common options: \code{"holm"}, \code{"fdr"}, \code{"bonferroni"}.
See \code{\link[stats]{p.adjust}} for all options.}

\item{include_controls}{Logical. If \code{TRUE}, control variables are included as rows
in the output alongside the predictors. Default is \code{FALSE}.}
}
\value{
A data frame with one row per predictor (and optionally per control variable).
Columns are \code{var} (variable name) followed by \code{r.<dv>}, \code{p.<dv>},
and \code{n.<dv>} for each outcome in \code{dv}.
}
\description{
Computes zero-order or partial correlations (controlling for a set of covariates)
between a set of predictor variables and one or more outcome variables. Returns a tidy
data frame with r, p, and n for each predictor-outcome pair.
}
\examples{
\dontrun{
# Zero-order correlations of two predictors with one outcome
partialR(data = mydata, predictors = c("x1", "x2"), dv = "y")

# Partial correlations controlling for age and sex
partialR(data = mydata, predictors = c("x1", "x2"), dv = "y",
         controls = c("age", "sex"))

# Multiple outcomes, Holm-adjusted p-values
partialR(data = mydata, predictors = c("x1", "x2", "x3"),
         dv = c("y1", "y2"), controls = c("age"), adjust = "holm")
}
}
\seealso{
\code{\link{setDepRDiffs}} for testing whether two outcomes differ in their
associations with a predictor set.
}
