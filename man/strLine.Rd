% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strLine.R
\name{strLine}
\alias{strLine}
\title{Proportion of Straight-Lined (Zero-Variability) Item Sets}
\usage{
strLine(x, group)
}
\arguments{
\item{x}{A numeric matrix or data frame in which each row is one rating
occasion (e.g., one person-situation-action) and each column is one of the
rated items.}

\item{group}{A vector of the same length as \code{nrow(x)} identifying the
respondent (or any other unit) over which to aggregate. Typically the
person ID column.}
}
\value{
A named numeric vector. Each element is the proportion of that
  respondent's rating occasions on which all items received the same value.
  Names are the unique values of \code{group}.
}
\description{
For respondents who rated the same set of items multiple times — e.g., across
situations, scenarios, or action options — return the proportion of those
rating occasions on which the respondent gave \emph{the same value to every
item in the set} (zero within-row variability).

Persistent straight-lining of this kind, especially when the items being
rated are heterogeneous in meaning, is a strong indicator of insufficient
effort responding.
}
\details{
Wood, Harms, Lowman, & DeSimone (2017) recommend treating respondents with
more than 10\% straight-lined rows as likely careless responders and
considering their removal. Less stringent thresholds (e.g., 25\%) may be
appropriate when the rated set is very short or when all items are
substantively similar in valence — both of which make zero within-row
variability less diagnostic.

Rows in which every rated item is \code{NA} are excluded from the per-row
SD computation and from the denominator for that respondent.
}
\examples{
# Two respondents rating 5 items across 4 occasions:
x <- rbind(
  c(2, 3, 4, 5, 3),   # r1 occasion 1: varied
  c(3, 3, 3, 3, 3),   # r1 occasion 2: STRAIGHT-LINED
  c(1, 4, 5, 2, 3),   # r1 occasion 3: varied
  c(2, 2, 2, 2, 2),   # r1 occasion 4: STRAIGHT-LINED
  c(4, 4, 4, 4, 4),   # r2 occasion 1: STRAIGHT-LINED
  c(1, 1, 1, 1, 1),   # r2 occasion 2: STRAIGHT-LINED
  c(5, 5, 5, 5, 5),   # r2 occasion 3: STRAIGHT-LINED
  c(2, 3, 1, 4, 2)    # r2 occasion 4: varied
)
grp <- rep(c("r1","r2"), each = 4)
strLine(x, group = grp)

# Flag at the Wood et al. (2017) 10\% threshold:
rates <- strLine(x, group = grp)
careless <- names(rates)[rates > 0.10]
}
\references{
Wood, D., Harms, P., Lowman, G. H., & DeSimone, J. A. (2017).
  Response speed and response consistency as mutually validating indicators
  of data quality in online samples. \emph{Social Psychological and
  Personality Science, 8}(4), 454-464.
}
